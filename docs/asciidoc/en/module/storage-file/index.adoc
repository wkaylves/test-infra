== File Storage Module

`test-infra-storage-file` provides MinIO / SFTP / FTP test infrastructure.

=== Maven Coordinates

[source,groovy,subs="attributes+"]
----
testImplementation 'io.github.wkaylves:test-infra-storage-file:{infraVersion}'
----

=== SharedMinIOContainer

MinIO singleton container, S3 API compatible.

[source,java]
----
class MinioIntegrationTest {
    @Container
    static GenericContainer<?> minio = SharedMinIOContainer.getInstance();

    @Test
    void shouldConnect() {
        String endpoint = SharedMinIOContainer.getEndpoint();
        // Connect to MinIO using endpoint
    }
}
----

=== Utility Class Summary

[cols="2,3",options="header"]
|===
|Class
|Description

|`SharedMinIOContainer`
|MinIO singleton container
|===
